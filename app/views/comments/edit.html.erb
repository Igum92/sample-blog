<h1>Edit comment</h1>

<%= form_for :comment, url: article_comment_path(@article, @comment), method: :put do |f| %>

	<p>
		<%= f.label :body %><br />
		<%= f.text_area :body %>
	</p>

	<p>
		<%= f.submit %>
	</p>

<% end %>