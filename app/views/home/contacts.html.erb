<h1>Contacts</h1>

<%= form_for :contact, url: '/contacts' do |f| %>
	<p>
		<%= f.label :email %><br/>
		<%= f.text_field :title %>
	</p>

	<p>
		<%= f.label :message %><br/>
		<%= f.text_area :text %>
	</p>

	<p>
		<%= f.submit %>
	</p>
<% end %>

